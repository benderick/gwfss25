_BASE_: competition_topowheat_trpl_tcpm.yaml

MODEL:
  TOPOWHEAT:
    BAZR:
      # Train detached high/low heads without enabling selective zoom in eval.
      ENABLED: False
      AUX_HEADS_ENABLED: True
